/**
 * ค่าคงที่จากการทดลองภาคสนาม (ฝั่งหลังบ้าน)
 * ========================================================================
 * ที่มาของตัวเลข: `meter-bill.xlsx` ที่ root ของโปรเจกต์ — มิเตอร์ 10 คู่ที่ติดตั้งชิดกัน
 * ภาพถ่าย 200 ภาพ (คู่ละ 20 ภาพ ด้านละ 10 ภาพ) · แผ่น "Statistical Summary" ตารางที่ 4.11
 * และแผ่น "รายงาน 3 สรุป" ตารางที่ 4.9 (ขอบเขตการตัดสินใจ)
 *
 * ไฟล์นี้คือคู่ของ `water-bill-web/src/app/core/measurement.constants.ts` ซึ่งเคยเขียน
 * ค้างไว้ว่า "ยังไม่มีไฟล์คู่กันที่ water-bill-service" — ทั้งสองไฟล์ต้องถือเลขชุดเดียวกัน
 *
 * ⚠️ ตัวเลขในไฟล์นี้เป็น **ผลการวัด** ไม่ใช่ค่าที่ปรับจูนได้ตามใจ จะแก้ได้ก็ต่อเมื่อ
 *    ทดลองซ้ำแล้วได้ค่าใหม่ และต้องแก้ที่ `meter-bill.xlsx` ให้ตรงกันด้วยเสมอ
 */

/**
 * เกณฑ์ค่าความเชื่อมั่นของ OCR ที่ระบบยอมให้ผ่านไปออกบิลเองได้
 *
 * ยืนยันซ้ำจากภาคสนามแล้ว (แผ่น Setup!B6 และตารางที่ 4.9): ภาพที่ conf ≥ 0.85 อ่านถูก
 * 15 จาก 16 ภาพ ส่วนภาพที่ต่ำกว่าเกณฑ์อ่านถูกเพียง 18 จาก 59 ภาพ เส้นนี้จึงแยกภาพ
 * ถูก/ผิดได้จริง ไม่ใช่เลขที่ตั้งไว้เฉย ๆ
 */
export const OCR_CONFIDENCE_THRESHOLD = 0.85;

/**
 * ความแม่นยำที่วัดได้จริงในแต่ละกลุ่มค่าความเชื่อมั่น (ตารางที่ 4.3ข)
 * ต้องตรงกับ `OCR_ACCURACY_BY_CONFIDENCE` ของหน้าเว็บคำต่อคำ
 */
export const OCR_ACCURACY_BY_CONFIDENCE = {
  /** conf ≥ 0.85 → อ่านถูก 15 จาก 16 ภาพ */
  atOrAboveThreshold: 0.938,
  /** conf < 0.85 → อ่านถูก 18 จาก 59 ภาพ */
  belowThreshold: 0.305,
  /** ไม่มีค่า conf → อ่านถูก 0 จาก 125 ภาพ */
  withoutConfidence: 0.0,
} as const;

/**
 * สมรรถนะ OCR ทั้งชุดที่วัดได้ (ตารางที่ 4.11)
 *
 * จุดสำคัญที่ต้องอ่านคู่กันเสมอ: ปัญหาหลักไม่ใช่ "อ่านเลขเพี้ยน" แต่คือ **ไม่ให้ค่าอ่าน
 * ออกมาเลย 125 จาก 200 ภาพ (62.5%)** ตัวเลขนี้คือเหตุผลที่ระบบต้องมีทางจับคู่บ้าน
 * ที่ไม่ต้องพึ่ง OCR (เลขมิเตอร์สะสม / sequence_index) ไม่ใช่พึ่ง OCR ทางเดียว
 */
export const OCR_FIELD_PERFORMANCE = {
  /** อ่านถูก 34 จาก 198 ภาพที่ตรวจผลแล้ว · 95% CI 12.6%–23.0% */
  accuracy: 0.165,
  /** TP ÷ (TP + FP) = 33 ÷ 75 — เมื่อระบบกล้าอ่าน จะอ่านถูกในสัดส่วนนี้ */
  precision: 0.44,
  /** TP ÷ (TP + FN) = 33 ÷ 198 */
  recall: 0.165,
  f1: 0.24,
  /** สัดส่วนภาพที่ระบบไม่คืนค่าอ่านออกมาเลย (125 จาก 200 ภาพ) */
  noReadRate: 0.625,
  /** ค่าเฉลี่ยของ confidence เฉพาะภาพที่มีค่า (SD 0.187 · n = 75) */
  meanConfidence: 0.67,
  /** ผ่านเกณฑ์ 0.85 เพียง 16 จาก 75 ภาพ — เงื่อนไขหน้างานยังห่างจากที่ระบบต้องการมาก */
  shareAboveThreshold: 0.213,
} as const;

/**
 * เงื่อนไขการถ่ายภาพที่พิสูจน์แล้วว่าเปลี่ยนความแม่นยำของ OCR (ตารางที่ 4.9)
 * เรียงตามน้ำหนักของผล: มุมเยื้องแกน X > ระยะถ่าย > มุมก้มแกน Y
 *
 * ส่งค่าชุดนี้ให้หน้าเว็บผ่าน API ได้เลย หน้าเว็บจะได้ไม่ต้องถือสำเนาของตัวเอง
 */
export const CAPTURE_LIMITS = {
  /**
   * ระยะถ่ายที่แนะนำ (เมตร) — ไม่เกิน 10 ซม.
   * ให้ความแม่นยำ 47.3% เทียบกับ 7.4% ที่ 11–40 ซม. และ 0.0% ที่เกิน 40 ซม.
   */
  maxDistanceM: 0.1,
  /**
   * มุมเยื้องซ้าย-ขวารอบแกน X สูงสุดที่ยอมรับได้ (องศา)
   * ปัจจัยที่ส่งผลชัดที่สุด: 24.8% ที่ไม่เกิน 10° เหลือ 3.2% เมื่อเกิน 10°
   * และภาพที่อ่านถูก 32 จาก 34 ภาพ (94.1%) อยู่ในช่วงไม่เกิน 10°
   */
  maxYawDeg: 10,
  /**
   * มุมก้มรอบแกน Y ขั้นต่ำที่แนะนำ (องศา) — มิเตอร์ประปาฝังระดับพื้น ต้องก้มกล้อง
   * ลงเกือบตั้งฉากกับหน้าปัด: 30.6% ที่เกิน 70° เทียบกับ 0.0% ที่ไม่เกิน 50°
   */
  minPitchDeg: 70,
} as const;

/**
 * ข้อสรุปเชิงออกแบบระบบจากผลการทดลอง — พิกัดใช้ "คัดกรองพื้นที่" ได้ แต่ตัดสินไม่ได้ว่า
 * เป็นมิเตอร์ตัวไหนในกลุ่มที่ติดกัน
 *
 * ═══ ตัวเลขที่เป็นเหตุผล ═══
 *   - ความคลาดเคลื่อนของพิกัดรายภาพเฉลี่ย 1.59 ม. เทียบกับระยะห่างจริงระหว่างมิเตอร์
 *     สองตัวที่วัดด้วยตลับเมตร 1.94 ม. — คิดเป็น ~82% ของปริมาณที่ต้องการวัด
 *     อัตราส่วนสัญญาณต่อสิ่งรบกวนใกล้ 1 เวกเตอร์ทิศทางจึงถูกกำหนดโดยความคลาดเคลื่อน
 *     ไม่น้อยไปกว่าโดยตำแหน่งจริง
 *   - Rank-1 Accuracy 81.0% (162/200) ฟังดูสูง แต่วัดแค่ว่า "หมุดที่ใกล้ที่สุดเป็นตัวเดียวกันไหม"
 *     ซึ่งผ่านง่ายเมื่อภาพจำนวนมากมีพิกัดซ้ำกัน (116 จาก 200 ภาพคลาด 0.000 ม. พอดี)
 *   - ทิศ 4 ทิศถูก 7 จาก 10 คู่ (70.0%) — แต่ตัวเลขนี้ **ไม่ใช่คำตอบของคำถาม**: คู่ที่ทิศจริง
 *     อยู่ในแกนเหนือ-ใต้ระบบตอบถูกทั้ง 6 คู่ ส่วนคู่ที่ทิศจริงเป็นซ้าย/ขวา ซึ่งเป็นสิ่งที่ระบบนี้
 *     ต้องแยกจริง ๆ ตอบถูกเพียง 1 จาก 4 คู่ (25.0%) เท่ากับเดาสุ่มพอดี (p = 0.684)
 *     ดูตารางที่ 4.2ข ของแผ่น "รายงาน 1 GPS" — ห้ามอ้าง 70% เดี่ยว ๆ โดยไม่แยกแกน
 *
 * ⚠️ ข้อจำกัดนี้เกิดจากความละเอียดของตัวเซนเซอร์ **ไม่ใช่จากการตั้งค่าซอฟต์แวร์** —
 *    ปรับ `directionTrustM` เป็นเท่าไรก็ไม่ทำให้พิกัดแยกมิเตอร์ที่ติดกันได้
 */
export const GPS_POLICY = {
  /** ความคลาดเคลื่อนของพิกัดรายภาพ เฉลี่ย 200 ภาพ (มัธยฐาน 0.00 · IQR 1.25 · SD 4.76) */
  meanErrorM: 1.587,
  /** ระยะห่างจริงระหว่างมิเตอร์สองตัวที่ติดกัน วัดด้วยตลับเมตร (พิสัย 0.02–5.80 ม.) */
  adjacentMeterSpacingM: 1.944,
  /**
   * รัศมีที่ยอมให้พิกัดในรูป "ยืนยัน" บ้านได้เอง (เมตร) — แผ่น Setup!B4
   * สั้นกว่ารัศมีที่ใช้เสนอบ้านให้คนเลือก (50 ม.) ครึ่งหนึ่ง เพราะด่านนี้ไม่มีคนตรวจซ้ำ
   */
  candidateRadiusM: 25,
  /**
   * ระยะขั้นต่ำที่ทิศซ้าย/ขวาจากพิกัด "เป็นข้อมูล" ไม่ใช่เสียงรบกวน (เมตร)
   *
   * ภาคสนามชี้ 15 ม.: มีเพียง 2 คู่จาก 10 คู่ (P06 และ P10) ที่หมุดห่างกันเกิน 15 ม.
   * จนพ้นเขตสัญญาณรบกวน อีก 8 คู่ตกอยู่ในเขตที่ทิศที่คำนวณได้เป็นการเดาทั้งหมด
   * (แผ่น Setup!B5 · ตารางที่ 4.9 แถวแรก)
   */
  directionTrustM: 15,
  /**
   * ห้ามเพิ่มฟีเจอร์ "เลือกมิเตอร์อัตโนมัติจากพิกัด" โดยไม่มีขั้นตอนให้เจ้าหน้าที่ยืนยัน
   * ตัวที่ตอบได้ว่าเป็นมิเตอร์ตัวไหนในกลุ่มที่ติดกันคือ `sequence_index` ที่จดไว้ล่วงหน้า
   * หรือเลขมิเตอร์สะสมบนหน้าปัด — ไม่ใช่พิกัด
   */
  allowAutoMeterSelection: false,
} as const;
